[package]
name = "marion-proto"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

# Transitional shim only. The wire vocabulary moved into `marion_core::proto`; this crate
# re-exports it so the move and the call-site rewrite stay separate commits, and is deleted by the
# rewrite commit.
[dependencies]
marion-core = { path = "../marion-core" }
